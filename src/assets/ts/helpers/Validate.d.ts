export default class Validate {
    private static PASSWORD_LENGTH;
    static isEmail(email: any): boolean;
    static isPassword(password: any, size?: number): boolean;
    static isEmpty(value: any): boolean;
    static isHTMLElement(element: any): boolean;
    static isGenericName(name: any): boolean;
}
//# sourceMappingURL=Validate.d.ts.map